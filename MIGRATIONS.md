# 🔄 Миграции базы данных

## 📋 Текущие миграции

В проекте есть 3 миграции:

1. **8c0b8a26d65b** - Initial migration (создание таблиц `users` и `orders`)
2. **c641d3a7f2eb** - Add foreign key to orders table
3. **2d9a89410994** - Add is_manager field to users

## 🚀 Применение миграций на Railway

### Вариант 1: Через Railway CLI (рекомендуется)

```bash
# 1. Установите Railway CLI (если ещё не установлен)
npm install -g @railway/cli

# 2. Войдите в аккаунт
railway login

# 3. Подключитесь к проекту
railway link

# 4. Примените миграции
railway run alembic upgrade head
```

### Вариант 2: Через Railway Shell (в браузере)

1. Откройте Railway Dashboard
2. Перейдите в сервис бота
3. Откройте вкладку **Settings**
4. Нажмите **Deploy** → **Shell**
5. В открывшейся консоли выполните:
```bash
alembic upgrade head
```

### Вариант 3: Автоматически при деплое

Можно добавить команду миграции в `railway.json`:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "alembic upgrade head && python -m app.main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

⚠️ **Внимание:** Этот вариант может вызвать проблемы при перезапуске бота.

## ✅ Проверка миграций

После применения миграций проверьте:

```bash
# Текущая версия БД
railway run alembic current

# История миграций
railway run alembic history
```

Вы должны увидеть:
```
2d9a89410994 (head)
```

## 🔍 Проверка таблиц в БД

Подключитесь к PostgreSQL:

```bash
railway connect postgres
```

Проверьте таблицы:

```sql
-- Список таблиц
\dt

-- Структура таблицы users
\d users

-- Структура таблицы orders
\d orders

-- Проверка данных
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM orders;
```

## 🆕 Создание новых миграций (для разработки)

Если вы изменили модели в `app/db/models.py`:

```bash
# Локально (через Docker)
docker-compose exec bot alembic revision --autogenerate -m "описание изменений"

# Или через Railway
railway run alembic revision --autogenerate -m "описание изменений"
```

Затем примените миграцию:

```bash
railway run alembic upgrade head
```

## 🔄 Откат миграций (если что-то пошло не так)

```bash
# Откатить последнюю миграцию
railway run alembic downgrade -1

# Откатить до конкретной версии
railway run alembic downgrade 8c0b8a26d65b

# Откатить все миграции
railway run alembic downgrade base
```

## 📝 Важные замечания

1. **Всегда делайте бэкап БД** перед применением миграций в production
2. **Проверяйте миграции локально** перед применением на Railway
3. **Не удаляйте файлы миграций** из папки `alembic/versions/`
4. **Применяйте миграции последовательно** - Alembic сам отслеживает порядок
5. **DATABASE_URL должен быть правильным** - используйте `postgresql+asyncpg://`

## 🐛 Решение проблем

### Ошибка: "Can't locate revision identified by..."

Это значит, что в БД есть версия, которой нет в файлах миграций.

**Решение:**
```bash
# Посмотрите текущую версию
railway run alembic current

# Сбросьте версию в БД
railway run alembic stamp head
```

### Ошибка: "Target database is not up to date"

**Решение:**
```bash
railway run alembic upgrade head
```

### Ошибка подключения к БД

Проверьте:
1. `DATABASE_URL` правильно указан в переменных окружения
2. Используется драйвер `postgresql+asyncpg://`
3. PostgreSQL сервис запущен в Railway

## 📚 Полезные команды Alembic

```bash
# Текущая версия БД
alembic current

# История всех миграций
alembic history

# Показать SQL без применения
alembic upgrade head --sql

# Применить до конкретной версии
alembic upgrade <revision_id>

# Показать разницу между версиями
alembic show <revision_id>
```

## 🎯 Чеклист применения миграций

- [ ] Railway PostgreSQL создан и запущен
- [ ] `DATABASE_URL` добавлен в переменные окружения бота
- [ ] `DATABASE_URL` использует `postgresql+asyncpg://`
- [ ] Бот успешно задеплоен
- [ ] Railway CLI установлен и настроен
- [ ] Выполнена команда `railway run alembic upgrade head`
- [ ] Проверена текущая версия: `railway run alembic current`
- [ ] Бот успешно запустился (проверить логи)
- [ ] Протестирован `/start` в Telegram

---

**После применения миграций бот готов к работе!** 🎉

