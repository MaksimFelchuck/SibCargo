# ✅ Чеклист перед деплоем на Railway

## 📦 Файлы для загрузки

Убедитесь, что эти файлы есть в проекте:

- [x] `Dockerfile` - конфигурация Docker
- [x] `railway.json` - конфигурация Railway
- [x] `requirements.txt` - зависимости Python
- [x] `alembic.ini` - конфигурация миграций
- [x] `alembic/` - папка с миграциями
- [x] `app/` - код приложения
- [x] `.dockerignore` - исключения для Docker
- [x] `README.md` - документация
- [x] `DEPLOY.md` - инструкция по деплою

## 🔐 Переменные окружения для Railway

Подготовьте эти значения:

```
BOT_TOKEN=<ваш_токен_от_BotFather>
MANAGER_CHAT_ID=<ваш_telegram_id>
DATABASE_URL=postgresql+asyncpg://user:pass@host:port/dbname
BASE_PRICE=500.0
PRICE_PER_KM=35.0
PRICE_PER_KG=2.0
```

## 📝 Порядок действий

### 1. Railway - PostgreSQL
- [ ] Создать новый проект в Railway
- [ ] Добавить PostgreSQL сервис
- [ ] Скопировать `DATABASE_URL`
- [ ] Заменить `postgresql://` на `postgresql+asyncpg://`

### 2. Railway - Bot Service
- [ ] Добавить новый сервис (GitHub или Empty)
- [ ] Загрузить код проекта
- [ ] Добавить все переменные окружения
- [ ] Дождаться успешного деплоя

### 3. Миграции
- [ ] Установить Railway CLI: `npm install -g @railway/cli`
- [ ] Войти: `railway login`
- [ ] Подключиться к проекту: `railway link`
- [ ] Применить миграции: `railway run alembic upgrade head`
- [ ] Проверить версию: `railway run alembic current`

📖 Подробная инструкция: [MIGRATIONS.md](MIGRATIONS.md)

### 4. Проверка
- [ ] Открыть логи в Railway Dashboard
- [ ] Найти строку "Бот запущен"
- [ ] Открыть бота в Telegram
- [ ] Отправить `/start`
- [ ] Проверить главное меню
- [ ] Создать тестовый заказ

## 🚫 НЕ загружайте

- ❌ `.env` файл (секреты должны быть в Railway)
- ❌ `venv/` папку (виртуальное окружение)
- ❌ `__pycache__/` (кэш Python)
- ❌ `.git/` (если загружаете ZIP)
- ❌ `docker-compose.yml` (для локальной разработки)

## 🎯 Готово к деплою!

Если все пункты выполнены, можете приступать к деплою на Railway.

Подробная инструкция: [DEPLOY.md](DEPLOY.md)

