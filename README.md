# SibCargo - Telegram-бот для грузоперевозок

Telegram-бот для заказа грузоперевозок по Новосибирску и области.

## 🚀 Быстрый старт

### Локальная разработка

#### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd SibCargo
```

#### 2. Настройка окружения
Скопируйте файл с примером переменных окружения:
```bash
cp env.example .env
```

Отредактируйте `.env` и укажите:
- `BOT_TOKEN` — токен вашего бота от [@BotFather](https://t.me/BotFather)
- `MANAGER_CHAT_ID` — ID чата/канала для получения заявок

#### 3. Запуск через Docker
```bash
docker-compose up --build
```

Бот запустится и будет доступен в Telegram.

### 🚂 Деплой на Railway

Подробная инструкция по деплою: [DEPLOY.md](DEPLOY.md)

**Быстрый старт:**
1. Создайте PostgreSQL базу в Railway
2. Создайте новый сервис и подключите GitHub репозиторий
3. Добавьте переменные окружения (`BOT_TOKEN`, `MANAGER_CHAT_ID`, `DATABASE_URL`)
4. Railway автоматически задеплоит бота
5. Примените миграции: `railway run alembic upgrade head`

## 📋 Текущий функционал (MVP v0.2)

✅ **Оформление заказа на перевозку:**
- 📅 Выбор даты загрузки через календарь (русский интерфейс)
- ⏰ Выбор времени загрузки
- 📍 Указание адреса загрузки (3 способа):
  - Ввод текстом с автоматическим геокодированием
  - Отправка текущей геолокации
  - Выбор точки на карте (как в Яндекс GO)
- 📍 Указание адреса выгрузки (аналогично)
- ⚖️ Указание веса груза
- 💰 Автоматический расчёт стоимости по реальному расстоянию
- ✅ Подтверждение и сохранение заказа в БД

✅ **Главное меню:**
- 🚚 **Оформить перевозку** — полный цикл оформления заказа
- ℹ️ **О нас** — информация о компании
- 📦 **Мои заказы** — просмотр истории заказов

✅ Команды:
- `/start` — запуск бота и показ главного меню
- `/help` — справка по использованию

## 🛠 Технологии

- **Python 3.11**
- **aiogram 3.x** — асинхронный фреймворк для Telegram Bot API
- **aiogram-calendar** — календарь для выбора даты
- **PostgreSQL** — база данных
- **SQLAlchemy** — ORM для работы с БД
- **Alembic** — миграции БД
- **geopy** — геокодирование и расчёт расстояний
- **Docker & Docker Compose** — контейнеризация

## 📁 Структура проекта

```
SibCargo/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа
│   ├── config.py            # Конфигурация
│   ├── db/                  # База данных
│   │   ├── __init__.py
│   │   ├── base.py          # Настройка SQLAlchemy
│   │   └── models.py        # Модели User и Order
│   ├── services/            # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── user_db_service.py    # Сервис для работы с пользователями
│   │   └── order_db_service.py   # Сервис для работы с заказами
│   ├── handlers/            # Обработчики команд и сообщений
│   │   ├── __init__.py
│   │   └── start.py
│   └── keyboards/           # Клавиатуры
│       ├── __init__.py
│       └── main_menu.py
├── alembic/                 # Миграции БД
│   ├── versions/
│   ├── env.py
│   └── script.py.mako
├── docs/                    # Документация
│   ├── DATABASE.md          # Описание БД
│   ├── SERVICES.md          # Описание сервисов
│   └── SQL_EXAMPLES.md      # Примеры SQL запросов
├── scripts/
│   └── migrate.sh           # Скрипт для работы с миграциями
├── requirements.txt
├── alembic.ini
├── Dockerfile
├── docker-compose.yml
├── env.example
└── README.md
```

## 🗄️ База данных

### Модели
- **User** — пользователи (клиенты)
  - telegram_id, username, first_name, last_name, phone
  - created_at, updated_at
  
- **Order** — заказы на грузоперевозку
  - Дата/время загрузки
  - Адреса и координаты загрузки/выгрузки
  - Вес груза, расстояние, стоимость
  - Статус заказа (draft, pending, confirmed, in_progress, completed, cancelled)
  - Комментарии клиента и менеджера

### Миграции (Alembic)

Создать новую миграцию:
```bash
docker-compose exec bot alembic revision --autogenerate -m "Description"
```

Применить миграции:
```bash
docker-compose exec bot alembic upgrade head
```

Откатить последнюю миграцию:
```bash
docker-compose exec bot alembic downgrade -1
```

Посмотреть историю миграций:
```bash
docker-compose exec bot alembic history
```

Проверить текущую версию БД:
```bash
docker-compose exec bot alembic current
```

## 🔜 Планы развития

- [ ] Реализация полного сценария оформления заказа (FSM)
- [ ] Интеграция календаря для выбора даты/времени
- [ ] Работа с геолокацией и адресами
- [ ] Расчёт стоимости перевозки
- [ ] Отправка заявок менеджеру
- [ ] История заказов пользователя

## 📝 Лицензия

Проект разработан для внутреннего использования.
