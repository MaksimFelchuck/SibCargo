# 🚂 Railway - Быстрый старт

## Шаг 1: Создание PostgreSQL

1. Зайдите на [railway.app](https://railway.app)
2. **New Project** → **Provision PostgreSQL**
3. Скопируйте `DATABASE_URL` из вкладки **Variables**
4. **ВАЖНО:** Замените `postgresql://` на `postgresql+asyncpg://`

## Шаг 2: Деплой бота

### Через GitHub (рекомендуется):
1. Загрузите проект на GitHub
2. **New** → **GitHub Repo** → выберите репозиторий
3. Railway автоматически обнаружит `Dockerfile`

### Через интерфейс:
1. **New** → **Empty Service**
2. **Settings** → **Source** → загрузите ZIP архив проекта

## Шаг 3: Переменные окружения

В настройках сервиса бота добавьте:

```
BOT_TOKEN=ваш_токен_от_BotFather
MANAGER_CHAT_ID=ваш_telegram_id
DATABASE_URL=postgresql+asyncpg://user:pass@host:port/dbname
BASE_PRICE=500.0
PRICE_PER_KM=35.0
PRICE_PER_KG=2.0
```

## Шаг 4: Миграции

После деплоя выполните:

```bash
railway run alembic upgrade head
```

Или через Railway Shell в интерфейсе.

## ✅ Готово!

Откройте бота в Telegram и отправьте `/start`

