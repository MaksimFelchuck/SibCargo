# 🚀 Инструкция по деплою на Railway

## 📋 Что нужно перед деплоем

1. Аккаунт на [Railway.app](https://railway.app/)
2. Токен Telegram бота (от @BotFather)
3. ID менеджера в Telegram

## 🗄️ Шаг 1: Создание PostgreSQL базы данных

1. Зайдите в Railway Dashboard
2. Создайте новый проект: **New Project** → **Provision PostgreSQL**
3. После создания БД, скопируйте `DATABASE_URL` из переменных окружения

## 🤖 Шаг 2: Деплой бота

### Вариант А: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub и загрузите туда проект
2. В Railway: **New Project** → **Deploy from GitHub repo**
3. Выберите ваш репозиторий
4. Railway автоматически обнаружит `Dockerfile` и начнёт деплой

### Вариант Б: Через CLI

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект
railway init

# Задеплойте
railway up
```

### Вариант В: Через интерфейс (загрузка файлов)

1. В Railway: **New Project** → **Empty Project**
2. Добавьте сервис: **New** → **Empty Service**
3. В настройках сервиса: **Settings** → **Source** → загрузите файлы проекта

## ⚙️ Шаг 3: Настройка переменных окружения

В настройках сервиса бота добавьте переменные:

```env
BOT_TOKEN=ваш_токен_от_BotFather
MANAGER_CHAT_ID=ваш_telegram_id
DATABASE_URL=postgresql+asyncpg://user:pass@host:port/dbname

# Pricing (опционально, есть значения по умолчанию)
BASE_PRICE=500.0
PRICE_PER_KM=35.0
PRICE_PER_KG=2.0
```

### Как получить DATABASE_URL:

1. Перейдите в сервис PostgreSQL
2. Вкладка **Variables**
3. Скопируйте значение `DATABASE_URL`
4. **ВАЖНО:** Замените `postgresql://` на `postgresql+asyncpg://`

Пример:
```
Было:     postgresql://user:pass@host:5432/railway
Стало:    postgresql+asyncpg://user:pass@host:5432/railway
```

## 🔄 Шаг 4: Миграции базы данных

После первого деплоя нужно применить миграции:

### Через Railway CLI:

```bash
# Подключитесь к проекту
railway link

# Выполните миграции
railway run alembic upgrade head
```

### Через Railway Shell:

1. Откройте сервис бота
2. Перейдите в **Settings** → **Deploy**
3. Нажмите **Shell**
4. Выполните команду:
```bash
alembic upgrade head
```

## ✅ Проверка деплоя

1. В Railway откройте логи сервиса бота
2. Должны увидеть:
```
Бот запущен
Start polling
Run polling for bot @your_bot_name
```

3. Откройте бота в Telegram и отправьте `/start`

## 🔧 Полезные команды Railway CLI

```bash
# Просмотр логов
railway logs

# Подключение к базе данных
railway connect postgres

# Выполнение команд в контейнере
railway run <command>

# Перезапуск сервиса
railway restart
```

## 📝 Структура проекта для Railway

```
SibCargo/
├── app/                    # Код приложения
├── alembic/               # Миграции БД
├── Dockerfile             # Конфигурация Docker
├── railway.json           # Конфигурация Railway
├── requirements.txt       # Зависимости Python
├── alembic.ini           # Конфигурация Alembic
└── .gitignore            # Игнорируемые файлы
```

## ⚠️ Важные замечания

1. **Не загружайте `.env` файл в Git!** Все секреты должны быть в переменных окружения Railway
2. **DATABASE_URL** должен использовать драйвер `asyncpg`: `postgresql+asyncpg://...`
3. После каждого изменения моделей БД нужно создавать и применять миграции
4. Railway автоматически перезапускает сервис при новом коммите в GitHub

## 🐛 Решение проблем

### Бот не запускается:
- Проверьте логи в Railway Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте правильность `BOT_TOKEN`

### Ошибки с базой данных:
- Убедитесь, что `DATABASE_URL` использует `postgresql+asyncpg://`
- Проверьте, что миграции применены (`alembic upgrade head`)
- Проверьте подключение к PostgreSQL сервису

### Бот отвечает медленно:
- Railway может "засыпать" на бесплатном плане
- Рассмотрите платный план для production

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Railway Dashboard
2. Проверьте документацию Railway: https://docs.railway.app/
3. Проверьте документацию aiogram: https://docs.aiogram.dev/

